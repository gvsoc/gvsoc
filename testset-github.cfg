from gvtest import *

# Called by gvtest to declare the tests
def testset_build(testset):

    testset.set_name('gvsoc')

    # Pulp examples moved to gvsoc/pulp/examples (imported from the
    # superproject testset.cfg).
